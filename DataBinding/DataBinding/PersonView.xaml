<Window x:Class="DataBinding.PersonView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PersonView" Height="350" Width="525">
    <Grid>
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition/>
        <RowDefinition/>
    </Grid.RowDefinitions>
        <TextBlock Text="First Name" Grid.Row="0"/>
        <TextBox Text="{Binding FirstName}" Grid.Row="1"/>
        <ListBox ItemsSource="{Binding NickNames}" Grid.Row="2"/>
        <Button Command="{Binding DoItCommand}" Grid.Row="3">Do It</Button>
    </Grid>
</Window>
