<Window x:Class="DataBinding.Views.PersonView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PersonView" Height="350" Width="525"
        DataContext="{Binding Source={StaticResource ViewModelLocator}, Path=PersonViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBox Text="{Binding FirstName}" />
        
        <Button Command="{Binding DoItCommand}" Grid.Row="1" Content="..."/>
    </Grid>
</Window>
